# P1-3/4/5 batch plan — 2026-05-20

## Fix A — POS empty catalog CTA (P1-3)
- File: `apps/web/src/components/sales/ProductPicker.tsx` + `apps/web/src/styles/sales-tab.css`.
- Add `useIonRouter` import; under the no-products branch render a terracotta button "Add your first product" that pushes `/{business.id}/products`. Only show when not searching.
- Add `.pos-empty__cta` class mirroring `.products-empty__cta` (tonal `:active` shading, no ink flash).
- Products tab empty state already wires `onAddProduct` → `handleOpenAdd` (opens AddProductModal in place). No change needed.
- i18n: add `sales.empty_state.no_products_cta` ("Add your first product") to all 11 locales.

## Fix B — Payment default-to-EXACT (P1-4)
- Files: `apps/web/src/components/sales/ViewCartModal.tsx`, `apps/web/src/components/sales/cart-modal/PaymentStep.tsx`, `apps/web/src/styles/sales-modal-cart.css`.
- ViewCartModal: when entering step 1 from step 0, prefill `tenderedStr = cart.total.toString()` and ensure method = `cash`. resetState already defaults method to cash. Use the existing `setStep(1)` path in the cart footer.
- PaymentStep: keep "Exact" chip behavior (set to total) but move it inline with denomination chips (same neutral border, no special terracotta outline). Make denomination chips ADD to current tendered. Add `is-selected` class with `var(--color-brand)` ring on the chip matching current tendered.
- Charge active from frame 1 follows from `tenderedSufficient` already true.
- No new i18n keys.

## Fix C — Welcome Continue never grey (P1-5)
- File: `apps/web/src/components/auth/wizard-steps/EmailStep.tsx`.
- Remove `disabled={!canSubmit}` and gate inside `handleSubmit`: if email invalid show inline `.auth-error` ("Enter a valid email") via the AuthField `below` slot (pattern already in use). Keep `disabled` only while `submitting`.
- i18n: add `auth.email_invalid` to all locales.

## Validation
- `npx tsc --noEmit` from `apps/web` after each fix.
- `npm run i18n:types --workspace=apps/web` once after JSON updates.
